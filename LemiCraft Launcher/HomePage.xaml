<Page x:Class="LemiCraft_Launcher.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Page.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="16,10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" 
                                   CornerRadius="3"
                                   Height="{TemplateBinding Height}"/>
                            <Border Name="PART_Track" 
                                   CornerRadius="3"
                                   Height="{TemplateBinding Height}">
                                <Border Name="PART_Indicator" 
                                       Background="{TemplateBinding Foreground}"
                                       CornerRadius="3"
                                       HorizontalAlignment="Left"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#374151" CornerRadius="4" Margin="0,2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NewsScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="3"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#374151" CornerRadius="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,10,0">
            <StackPanel>
                <TextBlock Text="Статус сервера" 
                          FontSize="18" 
                          FontWeight="SemiBold" 
                          Foreground="White"
                          Margin="0,0,0,20"/>

                <Border Background="#0F172A" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Версия Minecraft" 
                                   FontSize="11" 
                                   Foreground="#9CA3AF"
                                   Margin="0,0,0,6"/>
                        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock x:Name="ServerStatusDot"
                                       Text="●"
                                       FontSize="16"
                                       Foreground="#9CA3AF"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"
                                       ToolTip="Статус сервера"/>
                            <TextBlock x:Name="ServerVersionText"
                                       Text="1.21.10" 
                                       FontSize="16" 
                                       Foreground="White"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="#0F172A" CornerRadius="8" Padding="14" Margin="0,0,0,12">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Игроки онлайн" 
                                   FontSize="11" 
                                   Foreground="#9CA3AF"
                                   Margin="0,0,0,6"/>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock x:Name="OnlineText"
                                       Text="Проверка..."
                                       FontSize="16" 
                                       Foreground="#22C55E"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>

                            <ProgressBar x:Name="PlayersProgressBar"
                                         Grid.Column="1" 
                                         Value="0" 
                                         Maximum="100"
                                         Height="6"
                                         Margin="12,0,0,0"
                                         VerticalAlignment="Center"
                                         Background="#374151"
                                         Foreground="#22C55E"
                                         BorderThickness="0"/>
                        </Grid>
                    </Grid>
                </Border>

                <Border Background="#0F172A" CornerRadius="8" Padding="14" Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Версия сборки" 
                                  FontSize="11" 
                                  Foreground="#9CA3AF"
                                  Margin="0,0,0,6"/>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <TextBlock x:Name="ModpackStatusIcon"
                                      Text="●" 
                                      FontSize="16" 
                                      Foreground="#EF4444"
                                      VerticalAlignment="Center"
                                      Margin="0,0,8,0"/>
                            <TextBlock x:Name="ModpackStatus" 
                                      Text="Не установлена" 
                                      FontSize="16" 
                                      Foreground="White"
                                      FontWeight="SemiBold"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <TextBlock Text="Быстрые ссылки" 
                          FontSize="14" 
                          Foreground="#9CA3AF"
                          Margin="0,0,0,12"/>

                <Button Style="{StaticResource SecondaryButton}"
                       HorizontalAlignment="Stretch"
                       Margin="0,0,0,8"
                       Click="RulesButton_Click"
                       HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Правила сервера" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}"
                       HorizontalAlignment="Stretch"
                       Margin="0,0,0,8"
                       Click="WikiButton_Click"
                       HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📖" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Вики" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource SecondaryButton}"
                       HorizontalAlignment="Stretch"
                       Click="DiscordButton_Click"
                       HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💬" FontSize="16" Margin="0,0,10,0"/>
                        <TextBlock Text="Discord сервер" FontSize="14"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource Card}" Margin="10,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Новости и обновления" 
                  Grid.Row="0"
                  FontSize="18" 
                  FontWeight="SemiBold" 
                  Foreground="White"
                  Margin="0,0,0,20"/>

                <!-- Контейнер со скроллом и затуханием -->
                <Grid Grid.Row="1">
                    <ScrollViewer x:Name="NewsScrollViewer"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         PreviewMouseWheel="NewsScrollViewer_PreviewMouseWheel"
                         ScrollChanged="NewsScrollViewer_ScrollChanged"
                         Padding="0,0,10,0">
                        <ScrollViewer.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource NewsScrollBar}"/>
                        </ScrollViewer.Resources>

                        <StackPanel>
                            <!-- Твой контент новостей здесь -->
                            <TextBlock Text="Здесь будут новости..." 
                              Foreground="#9CA3AF"
                              TextWrapping="Wrap"/>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Затухание сверху -->
                    <Border x:Name="TopFade"
                   VerticalAlignment="Top"
                   Height="30"
                   Opacity="0"
                   IsHitTestVisible="False">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#1E293B" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <!-- Затухание снизу -->
                    <Border x:Name="BottomFade"
                   VerticalAlignment="Bottom"
                   Height="30"
                   Opacity="0"
                   IsHitTestVisible="False">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="#1E293B" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>
