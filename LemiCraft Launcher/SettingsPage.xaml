<Page x:Class="LemiCraft_Launcher.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent">

    <Page.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#22C55E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="20,12">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="20,12">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#22C55E"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#22C55E"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4B5563"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderRepeatButtonStyle" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}" Height="6" CornerRadius="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Background="#374151" 
                                   Height="6" 
                                   CornerRadius="3"
                                   VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" 
                                                Style="{StaticResource SliderRepeatButtonStyle}"
                                                Background="#22C55E"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="20" Height="20">
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Ellipse Fill="#22C55E" 
                                                        Stroke="White" 
                                                        StrokeThickness="2">
                                                    <Ellipse.Effect>
                                                        <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="0" Opacity="0.3"/>
                                                    </Ellipse.Effect>
                                                </Ellipse>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" 
                                                Style="{StaticResource SliderRepeatButtonStyle}"
                                                Background="Transparent"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                                         Background="{TemplateBinding Background}"
                                         BorderBrush="{TemplateBinding BorderBrush}"
                                         BorderThickness="{TemplateBinding BorderThickness}"
                                         Focusable="false"
                                         IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                         ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                               Background="{TemplateBinding Background}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="{TemplateBinding BorderThickness}"
                                               CornerRadius="8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="20" />
                                                </Grid.ColumnDefinitions>
                                                <Path x:Name="Arrow"
                                                     Grid.Column="1"
                                                     Fill="White"
                                                     HorizontalAlignment="Center"
                                                     VerticalAlignment="Center"
                                                     Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#4B5563"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#22C55E"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="12,10,32,10"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left" />
                            <Popup Name="Popup"
                                  Placement="Bottom"
                                  IsOpen="{TemplateBinding IsDropDownOpen}"
                                  AllowsTransparency="True"
                                  Focusable="False"
                                  PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                     SnapsToDevicePixels="True"
                                     MinWidth="{TemplateBinding ActualWidth}"
                                     MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                           Background="#1E293B"
                                           BorderThickness="1"
                                           BorderBrush="#374151"
                                           CornerRadius="8"
                                           Margin="0,2,0,0">
                                        <ScrollViewer Margin="4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border"
                               Background="{TemplateBinding Background}"
                               Padding="{TemplateBinding Padding}"
                               CornerRadius="6"
                               Margin="2">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#374151"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#22C55E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#374151" CornerRadius="4" Margin="0,2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="30,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="← Назад" 
                   Style="{StaticResource SecondaryButton}"
                   Padding="12,8"
                   FontSize="13"
                   Click="BackButton_Click"
                   Margin="0,0,16,0"/>
            <TextBlock Text="⚙️ Настройки лаунчера" 
                      FontSize="24" 
                      FontWeight="Bold" 
                      Foreground="White"
                      VerticalAlignment="Center"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <ScrollViewer x:Name="MainScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="0,0,12,0"
                          PreviewMouseWheel="MainScrollViewer_PreviewMouseWheel"
                          ScrollChanged="MainScrollViewer_ScrollChanged"
                          Focusable="True">

                <StackPanel>
                    <TextBlock Text="Параметры запуска" 
                          FontSize="16" 
                          FontWeight="SemiBold" 
                          Foreground="White"
                          Margin="0,0,0,16"/>

                    <Border Background="#1E293B" CornerRadius="10" Padding="18" Margin="0,0,0,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Выделенная память (RAM)" 
                                  FontSize="13" 
                                  FontWeight="Medium"
                                  Foreground="#E5E7EB"
                                  VerticalAlignment="Center"/>

                            <TextBlock x:Name="RamValueText"
                                  Grid.Column="1"
                                  Text="4 GB"
                                  FontSize="14"
                                  Foreground="#22C55E"
                                  FontWeight="SemiBold"
                                  VerticalAlignment="Center"/>

                            <Slider x:Name="RamSlider"
                                    Grid.Row="1"
                                    Grid.ColumnSpan="2"
                                    Style="{StaticResource ModernSlider}"
                                    Minimum="2"
                                    Maximum="16"
                               Value="4"
                               TickFrequency="1"
                               IsSnapToTickEnabled="True"
                               Margin="0,12,0,0"
                               ValueChanged="RamSlider_ValueChanged"/>
                        </Grid>
                    </Border>

                    <Border Background="#1E293B" CornerRadius="10" Padding="18" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Дополнительные JVM аргументы" 
                                  FontSize="13" 
                                  FontWeight="Medium"
                                  Foreground="#E5E7EB"
                                  Margin="0,0,0,10"/>
                            <TextBox x:Name="JvmArgsBox"
                                Style="{StaticResource ModernTextBox}"
                                Text="-XX:+UseG1GC -XX:+UnlockExperimentalVMOptions"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Пути к файлам" 
                          FontSize="16" 
                          FontWeight="SemiBold" 
                          Foreground="White"
                          Margin="0,0,0,16"/>

                    <Border Background="#1E293B" CornerRadius="10" Padding="18" Margin="0,0,0,12">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Путь к Java" 
                                  FontSize="13" 
                                  FontWeight="Medium"
                                  Foreground="#E5E7EB"
                                  Margin="0,0,0,10"/>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="JavaPathBox"
                                    Grid.Column="0"
                                    Style="{StaticResource ModernTextBox}"
                                    Text="Автоопределение"
                                    IsReadOnly="True"
                                    Margin="0,0,10,0"/>

                                <Button Grid.Column="1"
                                   Content="📁 Обзор"
                                   Style="{StaticResource SecondaryButton}"
                                   Click="BrowseJavaButton_Click"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <Border Background="#1E293B" CornerRadius="10" Padding="18" Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Папка игры" 
                                  FontSize="13" 
                                  FontWeight="Medium"
                                  Foreground="#E5E7EB"
                                  Margin="0,0,0,10"/>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="GamePathBox"
                                    Grid.Column="0"
                                    Style="{StaticResource ModernTextBox}"
                                    Text=".minecraft"
                                    IsReadOnly="True"
                                    Margin="0,0,10,0"/>

                                <Button Grid.Column="1"
                                   Content="📂 Изменить"
                                   Style="{StaticResource SecondaryButton}"
                                   Click="ChangeGamePathButton_Click"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <TextBlock Text="Дополнительно" 
                          FontSize="16" 
                          FontWeight="SemiBold" 
                          Foreground="White"
                          Margin="0,0,0,16"/>

                    <Border Background="#1E293B" CornerRadius="10" Padding="18">
                        <StackPanel>
                            <CheckBox x:Name="ShowLogsCheckBox"
                                  Content="Показывать окно с логами"
                                  Style="{StaticResource ModernCheckBox}"
                                  Margin="0,0,0,12"/>

                            <CheckBox x:Name="AutoConnectCheckBox"
                                  Content="Автоматически подключаться к серверу"
                                  Style="{StaticResource ModernCheckBox}"
                                  Margin="0,0,0,16"/>

                            <Border Height="1" Background="#374151" Margin="0,4,0,16"/>

                            <TextBlock Text="Поведение при запуске игры" 
                                   FontSize="13" 
                                   FontWeight="Medium"
                                   Foreground="#E5E7EB"
                                   Margin="0,0,0,8"/>

                            <ComboBox x:Name="LauncherBehaviorCombo" SelectedIndex="0">
                                <ComboBoxItem Content="Ничего не делать"/>
                                <ComboBoxItem Content="Закрыть лаунчер"/>
                                <ComboBoxItem Content="Скрыть лаунчер"/>
                            </ComboBox>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Rectangle x:Name="TopFade" Height="40" VerticalAlignment="Top" IsHitTestVisible="False"
                       Opacity="0" Margin="0,-1,20,0">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#FF0F172A" Offset="0"/>
                        <GradientStop Color="#000F172A" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Rectangle x:Name="BottomFade" Height="40" VerticalAlignment="Bottom" IsHitTestVisible="False"
                       Opacity="0" Margin="0,0,20,-1">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                        <GradientStop Color="#FF0F172A" Offset="0"/>
                        <GradientStop Color="#000F172A" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Grid>

        <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
            <Button Content="Сбросить"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0"
                        Click="ResetButton_Click"/>
            <Button Content="Сохранить"
                        Style="{StaticResource ModernButton}"
                        Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
