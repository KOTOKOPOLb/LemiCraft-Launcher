<Window x:Class="LemiCraft_Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LemiCraft Launcher" 
        Height="720" 
        Width="1100"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Storyboard x:Key="WindowFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                           From="0.95" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                           From="0.95" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="WindowFadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.2"/>
        </Storyboard>

        <Storyboard x:Key="PageFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="PageFadeOut">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.2"/>
        </Storyboard>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#22C55E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                Padding="24,14">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                                <Setter Property="Foreground" Value="#6B7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#374151"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="16,10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.RenderTransform>
        <ScaleTransform ScaleX="1" ScaleY="1" CenterX="550" CenterY="350"/>
    </Window.RenderTransform>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowFadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="#0F172A" CornerRadius="16" ClipToBounds="True" BorderBrush="#374151" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1E293B" CornerRadius="16,16,0,0" 
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="24,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Ellipse Width="36" Height="36" Fill="#22C55E" Margin="0,0,12,0">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="#22C55E" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="L" 
                                  FontSize="20" 
                                  FontWeight="Bold" 
                                  Foreground="White"
                                  VerticalAlignment="Center"
                                  Margin="-42,0,20,0"/>
                        <TextBlock Text="LemiCraft Launcher" 
                                  FontSize="16" 
                                  FontWeight="Medium"
                                  Foreground="White" 
                                  VerticalAlignment="Center"/>
                        <Border Background="#0F172A"
                               Padding="8,4"
                               CornerRadius="4"
                               Margin="12,0,0,0"
                               VerticalAlignment="Center">
                            <TextBlock Text="BETA" 
                                      FontSize="10" 
                                      FontWeight="Bold"
                                      Foreground="#22C55E"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#22C55E" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </Border>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="—" Style="{StaticResource IconButton}" Click="MinimizeButton_Click"/>
                        <Button Content="✕" Style="{StaticResource IconButton}" Click="CloseButton_Click" Margin="4,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Frame x:Name="MainFrame" 
                   Grid.Row="1"
                   NavigationUIVisibility="Hidden"
                   Background="Transparent"
                   Navigated="MainFrame_Navigated"/>

            <Border x:Name="Footer" Grid.Row="2" Background="#1E293B" CornerRadius="0,0,16,16">
                <Grid Margin="30,0" Height="90">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel x:Name="AccountInfoPanel"
                   Grid.Column="0"
                   Orientation="Horizontal" 
                   VerticalAlignment="Center"
                   Cursor="Hand"
                   MouseLeftButtonDown="AccountInfo_Click">

                        <StackPanel.ContextMenu>
                            <ContextMenu x:Name="AccountMenu">
                            </ContextMenu>
                        </StackPanel.ContextMenu>

                        <Border x:Name="PlayerAvatar"
                    Width="48" 
                    Height="48" 
                    CornerRadius="8"
                    Margin="0,0,14,0"
                    ClipToBounds="True">
                            <Border.Background>
                                <SolidColorBrush Color="#374151"/>
                            </Border.Background>

                            <TextBlock x:Name="PlayerAvatarEmoji"
                           Text="👤" 
                           FontSize="24" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"/>
                        </Border>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="UsernameText" 
                          Text="Не авторизован" 
                          FontSize="14" 
                          Foreground="White"
                          FontWeight="SemiBold"/>
                            <TextBlock x:Name="AccountTypeText" 
                          Text="Нажмите для входа" 
                          FontSize="12" 
                          Foreground="#22C55E"/>
                        </StackPanel>
                    </StackPanel>

                    <Border x:Name="FooterProgressPanel"
                Grid.Column="1"
                Visibility="Collapsed"
                Background="#0F172A"
                CornerRadius="8"
                Padding="16,10"
                Margin="20,0">
                        <Grid>
                            <StackPanel>
                                <TextBlock x:Name="FooterProgressText" 
                              Text="Подготовка..." 
                              Foreground="#9CA3AF" 
                              FontSize="11" 
                              Margin="0,0,0,4"/>
                                <ProgressBar x:Name="FooterProgressBar" 
                                Height="4" 
                                Maximum="100" 
                                Value="0" 
                                Background="#374151" 
                                Foreground="#22C55E" 
                                BorderThickness="0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center">
                                <TextBlock x:Name="FooterFilesText" 
                              Text="0 / 0" 
                              Foreground="#6B7280" 
                              FontSize="10" 
                              Margin="0,0,8,0"/>
                                <TextBlock x:Name="FooterPercentText" 
                              Text="0%" 
                              Foreground="#6B7280" 
                              FontSize="10"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <StackPanel x:Name="ActionButtonsPanel"
                   Grid.Column="2"
                   Orientation="Horizontal" 
                   VerticalAlignment="Center">
                        <Button x:Name="SettingsButton"
                   Content="⚙️ Настройки" 
                   Style="{StaticResource SecondaryButton}"
                   Margin="0,0,12,0"
                   Click="SettingsButton_Click"/>

                        <Button x:Name="PlayButton"
                     Style="{StaticResource PrimaryButton}"
                     Width="220"
                     Height="54"
                     FontSize="17"
                     Click="PlayButton_Click"
                     IsEnabled="False">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock x:Name="FooterPlayButtonIcon" Text="▶️" FontSize="18" Margin="0,0,10,0"/>
                                <TextBlock x:Name="FooterPlayButtonText" Text="Играть" FontSize="16"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
