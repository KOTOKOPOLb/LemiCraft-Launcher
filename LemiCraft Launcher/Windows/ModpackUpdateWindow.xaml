<Window x:Class="LemiCraft_Launcher.Windows.ModpackUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление сборки"
        Height="650"
        Width="600"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">

    <Border x:Name="RootBorder"
            Background="#0F172A"
            CornerRadius="16"
            BorderBrush="#374151"
            BorderThickness="1"
            RenderTransformOrigin="0.5,0.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#1E293B"
                    CornerRadius="16,16,0,0"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="24,18">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7B8;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="18"
                                   Foreground="White"
                                   Margin="0,0,8,0"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Обновление сборки"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="×"
                            x:Name="CloseButtonTop"
                            Width="32"
                            Height="32"
                            HorizontalAlignment="Right"
                            FontSize="20"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="28,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#1E293B"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Версия"
                                   FontSize="13"
                                   Foreground="#6B7280"
                                   Margin="0,0,0,8"/>
                        <TextBlock x:Name="VersionText"
                                   Text="0.0.0 → 2.0.0"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="White"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="1"
                           Text="Тип обновления"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#9CA3AF"
                           Margin="0,0,0,12"/>

                <StackPanel Grid.Row="2">
                    <Border x:Name="FullUpdateCard"
                            Background="#1E293B"
                            CornerRadius="12"
                            Padding="16"
                            Cursor="Hand"
                            MouseLeftButtonDown="FullUpdateOption_Click"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Width="20"
                                    Height="20"
                                    BorderBrush="#60A5FA"
                                    BorderThickness="2"
                                    CornerRadius="10"
                                    Margin="0,0,12,0">
                                <Ellipse x:Name="FullUpdateCheckMark"
                                         Width="10"
                                         Height="10"
                                         Fill="#60A5FA"
                                         Visibility="Visible"/>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Полное обновление"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Рекомендуется • Обновляет все компоненты"
                                           FontSize="12"
                                           Foreground="#9CA3AF"/>
                            </StackPanel>

                            <TextBlock x:Name="FullUpdateSize"
                                       Grid.Column="2"
                                       Text="75,6 MB"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#60A5FA"
                                       VerticalAlignment="Center"/>

                            <RadioButton x:Name="FullUpdateRadio"
                                         GroupName="UpdateType"
                                         IsChecked="True"
                                         Visibility="Collapsed"
                                         Checked="UpdateTypeChanged"/>
                        </Grid>
                    </Border>

                    <Border x:Name="ModsAndResourcesCard"
                            Background="#1E293B"
                            CornerRadius="12"
                            Padding="16"
                            Cursor="Hand"
                            MouseLeftButtonDown="ModsAndResourcesOption_Click"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Width="20"
                                    Height="20"
                                    BorderBrush="#60A5FA"
                                    BorderThickness="2"
                                    CornerRadius="10"
                                    Margin="0,0,12,0">
                                <Ellipse x:Name="ModsAndResourcesCheckMark"
                                         Width="10"
                                         Height="10"
                                         Fill="#60A5FA"
                                         Visibility="Collapsed"/>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Моды и ресурспаки"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Обновляет моды и ресурспаки"
                                           FontSize="12"
                                           Foreground="#9CA3AF"/>
                            </StackPanel>

                            <TextBlock x:Name="ModsAndResourcesSize"
                                       Grid.Column="2"
                                       Text="60,5 MB"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#60A5FA"
                                       VerticalAlignment="Center"/>

                            <RadioButton x:Name="ModsAndResourcesRadio"
                                         GroupName="UpdateType"
                                         Visibility="Collapsed"
                                         Checked="UpdateTypeChanged"/>
                        </Grid>
                    </Border>

                    <Border x:Name="ModsOnlyCard"
                            Background="#1E293B"
                            CornerRadius="12"
                            Padding="16"
                            Cursor="Hand"
                            MouseLeftButtonDown="ModsOnlyOption_Click"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Width="20"
                                    Height="20"
                                    BorderBrush="#60A5FA"
                                    BorderThickness="2"
                                    CornerRadius="10"
                                    Margin="0,0,12,0">
                                <Ellipse x:Name="ModsOnlyCheckMark"
                                         Width="10"
                                         Height="10"
                                         Fill="#60A5FA"
                                         Visibility="Collapsed"/>
                            </Border>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Только моды"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Самое быстрое • Минимальная загрузка"
                                           FontSize="12"
                                           Foreground="#9CA3AF"/>
                            </StackPanel>

                            <TextBlock x:Name="ModsOnlySize"
                                       Grid.Column="2"
                                       Text="45,4 MB"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="#60A5FA"
                                       VerticalAlignment="Center"/>

                            <RadioButton x:Name="ModsOnlyRadio"
                                         GroupName="UpdateType"
                                         Visibility="Collapsed"
                                         Checked="UpdateTypeChanged"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <Border x:Name="ProgressPanel"
                        Grid.Row="3"
                        Background="#1E293B"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,20,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="ProgressText"
                                   Text="Скачивание..."
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,12"
                                   HorizontalAlignment="Center"/>

                        <ProgressBar x:Name="UpdateProgress"
                                     Height="8"
                                     Minimum="0"
                                     Maximum="100"
                                     Value="0"
                                     BorderThickness="0"
                                     Background="#374151"
                                     Foreground="#60A5FA">
                            <ProgressBar.Template>
                                <ControlTemplate TargetType="ProgressBar">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4">
                                        <Border x:Name="PART_Track"
                                                CornerRadius="4">
                                            <Border x:Name="PART_Indicator"
                                                    Background="{TemplateBinding Foreground}"
                                                    HorizontalAlignment="Left"
                                                    CornerRadius="4"/>
                                        </Border>
                                    </Border>
                                </ControlTemplate>
                            </ProgressBar.Template>
                        </ProgressBar>

                        <TextBlock Text="Пожалуйста, не закрывайте это окно"
                                   FontSize="12"
                                   Foreground="#6B7280"
                                   Margin="0,12,0,0"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="4" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="CancelButton"
                            Grid.Column="0"
                            Content="Отмена"
                            Height="44"
                            MinWidth="140"
                            Padding="20,0"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            Foreground="White"
                            BorderBrush="#374151"
                            BorderThickness="1"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="UpdateButton"
                            Grid.Column="1"
                            Content="Обновить"
                            Height="44"
                            MinWidth="180"
                            Padding="24,0"
                            Background="#22C55E"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="UpdateButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
