<Window x:Class="LemiCraft_Launcher.Windows.ModpackUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление сборки"
        Height="550"
        Width="650"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">

    <Border x:Name="RootBorder"
            Background="#0F172A"
            CornerRadius="16"
            BorderBrush="#374151"
            BorderThickness="1"
            RenderTransformOrigin="0.5,0.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0"
                    Background="#1E293B"
                    CornerRadius="16,16,0,0"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="24,18">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📦" 
                                   FontSize="18" 
                                   Margin="0,0,8,0"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Обновление сборки"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button Content="×"
                            x:Name="CloseButtonTop"
                            Width="32"
                            Height="32"
                            HorizontalAlignment="Right"
                            FontSize="20"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          Padding="28,24">
                <StackPanel>
                    <!-- Version Info -->
                    <Border Background="#1E293B"
                            CornerRadius="12"
                            Padding="20"
                            Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="Информация об обновлении"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="#9CA3AF"
                                       Margin="0,0,0,12"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="Версия:"
                                           Grid.Column="0"
                                           FontSize="14"
                                           Foreground="#6B7280"
                                           Margin="0,0,12,0"/>

                                <TextBlock x:Name="VersionText"
                                           Grid.Column="1"
                                           Text="1.0.0 → 1.1.0"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Update Options -->
                    <TextBlock Text="Тип обновления"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#9CA3AF"
                               Margin="0,0,0,12"/>

                    <!-- Full Update -->
                    <Border Background="#1E293B"
                            CornerRadius="12"
                            Padding="16"
                            Margin="0,0,0,12"
                            Cursor="Hand"
                            MouseLeftButtonDown="FullUpdateOption_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <RadioButton x:Name="FullUpdateRadio"
                                         Grid.Column="0"
                                         IsChecked="True"
                                         VerticalAlignment="Center"
                                         Margin="0,0,16,0">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border Width="20" Height="20"
                                                CornerRadius="10"
                                                BorderBrush="#374151"
                                                BorderThickness="2"
                                                Background="Transparent">
                                            <Ellipse x:Name="CheckMark"
                                                     Width="10" Height="10"
                                                     Fill="#60A5FA"
                                                     Visibility="Collapsed"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Полное обновление"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Рекомендуется • Обновляет все компоненты"
                                           FontSize="12"
                                           Foreground="#6B7280"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock x:Name="FullUpdateSize"
                                           Text="150 MB"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="#60A5FA"
                                           HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Mods + Resources Update -->
                    <Border Background="#1E293B"
                            CornerRadius="12"
                            Padding="16"
                            Margin="0,0,0,12"
                            Cursor="Hand"
                            MouseLeftButtonDown="ModsAndResourcesOption_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <RadioButton x:Name="ModsAndResourcesRadio"
                                         Grid.Column="0"
                                         VerticalAlignment="Center"
                                         Margin="0,0,16,0">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border Width="20" Height="20"
                                                CornerRadius="10"
                                                BorderBrush="#374151"
                                                BorderThickness="2"
                                                Background="Transparent">
                                            <Ellipse x:Name="CheckMark"
                                                     Width="10" Height="10"
                                                     Fill="#60A5FA"
                                                     Visibility="Collapsed"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Моды и ресурспаки"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Обновляет моды и ресурспаки"
                                           FontSize="12"
                                           Foreground="#6B7280"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock x:Name="ModsAndResourcesSize"
                                           Text="100 MB"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="#60A5FA"
                                           HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Mods Only Update -->
                    <Border Background="#1E293B"
                            CornerRadius="12"
                            Padding="16"
                            Cursor="Hand"
                            MouseLeftButtonDown="ModsOnlyOption_Click">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <RadioButton x:Name="ModsOnlyRadio"
                                         Grid.Column="0"
                                         VerticalAlignment="Center"
                                         Margin="0,0,16,0">
                                <RadioButton.Template>
                                    <ControlTemplate TargetType="RadioButton">
                                        <Border Width="20" Height="20"
                                                CornerRadius="10"
                                                BorderBrush="#374151"
                                                BorderThickness="2"
                                                Background="Transparent">
                                            <Ellipse x:Name="CheckMark"
                                                     Width="10" Height="10"
                                                     Fill="#60A5FA"
                                                     Visibility="Collapsed"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </RadioButton.Template>
                            </RadioButton>

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="Только моды"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="White"
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="Быстрое обновление модов"
                                           FontSize="12"
                                           Foreground="#6B7280"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock x:Name="ModsOnlySize"
                                           Text="50 MB"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="#60A5FA"
                                           HorizontalAlignment="Right"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Progress Panel -->
                    <Border x:Name="ProgressPanel"
                            Background="#1E293B"
                            CornerRadius="12"
                            Padding="20"
                            Margin="0,24,0,0"
                            Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock x:Name="ProgressText"
                                       Text="Скачивание..."
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       Margin="0,0,0,12"
                                       HorizontalAlignment="Center"/>

                            <ProgressBar x:Name="UpdateProgress"
                                         Height="8"
                                         Minimum="0"
                                         Maximum="100"
                                         Value="0"
                                         BorderThickness="0"
                                         Background="#374151"
                                         Foreground="#60A5FA">
                                <ProgressBar.Template>
                                    <ControlTemplate TargetType="ProgressBar">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="4">
                                            <Border x:Name="PART_Track"
                                                    CornerRadius="4">
                                                <Border x:Name="PART_Indicator"
                                                        Background="{TemplateBinding Foreground}"
                                                        HorizontalAlignment="Left"
                                                        CornerRadius="4"/>
                                            </Border>
                                        </Border>
                                    </ControlTemplate>
                                </ProgressBar.Template>
                            </ProgressBar>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Action Buttons -->
            <Border Grid.Row="2"
                    Background="#1E293B"
                    CornerRadius="0,0,16,16"
                    BorderBrush="#374151"
                    BorderThickness="0,1,0,0"
                    Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="CancelButton"
                            Grid.Column="0"
                            Content="Отмена"
                            Height="44"
                            MinWidth="140"
                            Padding="20,0"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            Foreground="White"
                            BorderBrush="#374151"
                            BorderThickness="1"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="UpdateButton"
                            Grid.Column="1"
                            Content="Обновить"
                            Height="44"
                            MinWidth="180"
                            Padding="24,0"
                            Background="#60A5FA"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="UpdateButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#3B82F6"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
