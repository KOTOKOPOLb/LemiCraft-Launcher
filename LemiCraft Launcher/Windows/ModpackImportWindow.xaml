<Window x:Class="LemiCraft_Launcher.Windows.ModpackImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт сборки"
        Height="580"
        Width="520"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        Opacity="0"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Storyboard x:Key="WindowFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                             From="14" To="0" Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="Transparent" CornerRadius="3"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Background="#4B5563" CornerRadius="3" Margin="0,2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.RenderTransform>
        <TranslateTransform Y="0"/>
    </Window.RenderTransform>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowFadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border Background="#0F172A"
            CornerRadius="16"
            BorderBrush="#374151"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#1E293B"
                    CornerRadius="16,16,0,0"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="24,18">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7B8;"
                                   FontFamily="Segoe MDL2 Assets"
                                   FontSize="16"
                                   Foreground="White"
                                   Margin="0,0,10,0"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Импорт сборки по коду"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Content="×"
                            Width="32" Height="32"
                            HorizontalAlignment="Right"
                            FontSize="20"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="28,24,28,28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#1E293B"
                        CornerRadius="12"
                        Padding="16"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Код импорта"
                                   FontSize="12"
                                   Foreground="#9CA3AF"
                                   Margin="0,0,0,8"/>

                        <Border Background="#0F172A"
                                CornerRadius="8"
                                BorderBrush="#374151"
                                BorderThickness="1">
                            <TextBox x:Name="CodeInput"
                                     Background="Transparent"
                                     Foreground="White"
                                     CaretBrush="White"
                                     BorderThickness="0"
                                     FontSize="26"
                                     FontWeight="Bold"
                                     Padding="14,10"
                                     MaxLength="10"
                                     VerticalContentAlignment="Center"
                                     KeyDown="CodeInput_KeyDown">
                                <TextBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"/>
                                    </Style>
                                </TextBox.Resources>
                            </TextBox>
                        </Border>

                        <TextBlock Text="Код можно найти на lemicraft.ru/mods после нажатия «Импорт»"
                                   FontSize="11"
                                   Foreground="#6B7280"
                                   Margin="0,8,0,12"
                                   TextWrapping="Wrap"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button x:Name="OpenSiteBtn"
                                    Grid.Column="0"
                                    Height="42"
                                    Background="#1E3A5F"
                                    Foreground="#60A5FA"
                                    BorderThickness="0"
                                    FontSize="13"
                                    FontWeight="Medium"
                                    Cursor="Hand"
                                    Click="OpenSiteBtn_Click">
                                <Button.Content>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE774;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="13"
                                                   Foreground="#60A5FA"
                                                   Margin="0,0,7,0"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="Открыть сборщик" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button.Content>
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#1E40AF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>

                            <Button x:Name="PreviewBtn"
                                    Grid.Column="2"
                                    Height="42"
                                    Background="#22C55E"
                                    Foreground="White"
                                    BorderThickness="0"
                                    FontSize="14"
                                    FontWeight="SemiBold"
                                    Cursor="Hand"
                                    Click="PreviewBtn_Click">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Background" Value="#374151"/>
                                                <Setter Property="Foreground" Value="#6B7280"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                                <Button.Content>Загрузить</Button.Content>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Border>

                <TextBlock x:Name="ModsHeader"
                           Grid.Row="1"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#9CA3AF"
                           Margin="0,0,0,8"
                           Visibility="Collapsed"/>

                <Grid Grid.Row="2">
                    <Border Background="#1E293B"
                            CornerRadius="12"
                            ClipToBounds="True">
                        <Grid>
                            <StackPanel x:Name="IdlePanel"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <TextBlock Text="&#xE8C8;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="36"
                                           Foreground="#4B5563"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,12"/>
                                <TextBlock Text="Введите код и нажмите «Загрузить»"
                                           FontSize="13"
                                           Foreground="#6B7280"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel x:Name="LoadingPanel"
                                        Visibility="Collapsed"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <TextBlock Text="&#xE916;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="32"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,12"/>
                                <TextBlock Text="Загрузка данных..."
                                           FontSize="13"
                                           Foreground="#9CA3AF"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <StackPanel x:Name="ErrorPanel"
                                        Visibility="Collapsed"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center">
                                <TextBlock Text="&#xE783;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="32"
                                           Foreground="#EF4444"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,12"/>
                                <TextBlock x:Name="ErrorText"
                                           Text="Код не найден или истёк"
                                           FontSize="13"
                                           Foreground="#EF4444"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>

                            <ScrollViewer x:Name="ModListScroll"
                                          Visibility="Collapsed"
                                          VerticalScrollBarVisibility="Auto"
                                          HorizontalScrollBarVisibility="Disabled"
                                          Padding="12,2,6,8"
                                          PreviewMouseWheel="ModListScroll_PreviewMouseWheel"
                                          ScrollChanged="ModListScroll_ScrollChanged">
                                <ItemsControl x:Name="ModList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#0F172A"
                                                    CornerRadius="8"
                                                    Padding="12,8"
                                                    Margin="0,4,0,0">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding Name}"
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="White"
                                                               VerticalAlignment="Center"/>
                                                    <TextBlock Grid.Column="1"
                                                               Text="{Binding Version}"
                                                               FontSize="11"
                                                               Foreground="#6B7280"
                                                               VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>

                    <Rectangle x:Name="TopFade"
                               Height="44"
                               VerticalAlignment="Top"
                               IsHitTestVisible="False"
                               Opacity="0"
                               Margin="10,0.3,22,0">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FF1E293B" Offset="0"/>
                                <GradientStop Color="#FF1E293B" Offset="0.5"/>
                                <GradientStop Color="#001E293B" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>

                    <Rectangle x:Name="BottomFade"
                               Height="44"
                               VerticalAlignment="Bottom"
                               IsHitTestVisible="False"
                               Opacity="0"
                               Margin="10,0,22,1">
                        <Rectangle.Fill>
                            <LinearGradientBrush StartPoint="0,1" EndPoint="0,0">
                                <GradientStop Color="#FF1E293B" Offset="0"/>
                                <GradientStop Color="#FF1E293B" Offset="0.5"/>
                                <GradientStop Color="#001E293B" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Grid>

                <Border x:Name="ProgressPanel"
                        Grid.Row="3"
                        Background="#1E293B"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,16,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="ProgressText"
                                   Text="Скачивание..."
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   Margin="0,0,0,10"
                                   HorizontalAlignment="Center"/>
                        <ProgressBar x:Name="ImportProgress"
                                     Height="8"
                                     Minimum="0"
                                     Maximum="100"
                                     Value="0"
                                     BorderThickness="0"
                                     Background="#374151"
                                     Foreground="#22C55E">
                            <ProgressBar.Template>
                                <ControlTemplate TargetType="ProgressBar">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <Border x:Name="PART_Track" CornerRadius="4">
                                            <Border x:Name="PART_Indicator"
                                                    Background="{TemplateBinding Foreground}"
                                                    HorizontalAlignment="Left"
                                                    CornerRadius="4"/>
                                        </Border>
                                    </Border>
                                </ControlTemplate>
                            </ProgressBar.Template>
                        </ProgressBar>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="4" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="CancelButton"
                            Grid.Column="0"
                            Content="Отмена"
                            Height="44"
                            MinWidth="120"
                            Padding="20,0"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            Foreground="White"
                            BorderBrush="#374151"
                            BorderThickness="1"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="ImportBtn"
                            Grid.Column="1"
                            Content="Импортировать"
                            Height="44"
                            MinWidth="160"
                            Padding="24,0"
                            Background="#22C55E"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="14"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            IsEnabled="False"
                            Click="ImportBtn_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                        <Setter Property="Foreground" Value="#6B7280"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
